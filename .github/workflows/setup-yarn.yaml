name: setup-yarn
on:
  workflow_call:
jobs:
  setup-yarn:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: corepack enable yarn
      - run: yarn
