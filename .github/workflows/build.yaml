name: build
on:
  push:
    branches-ignore:
      - 'save*'
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/setup-yarn.yaml
      - run: yarn build-release
