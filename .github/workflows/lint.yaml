name: lint
on:
  push:
    branches-ignore:
      - 'save*'
  pull_request:
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: ./.github/workflows/setup-yarn.yaml
      - run: yarn lint
